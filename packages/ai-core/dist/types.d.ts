/**
 * @causality/ai-core — Type Definitions
 *
 * Types for AI model interaction and insight generation.
 */
import type { HistoricalReport, HistoricalReportItem } from '@causality/historical-core';
export type { HistoricalReport, HistoricalReportItem };
/**
 * Configuration for the AI Core.
 */
export interface AIConfig {
    /** Model provider name (e.g., 'openai', 'gemini', 'local') */
    readonly provider: string;
    /** Model name (e.g., 'gpt-4', 'gemini-pro') */
    readonly model: string;
    /** API key (optional depending on provider) */
    readonly apiKey?: string;
    /** Base URL (for local models) */
    readonly baseUrl?: string;
    /** Max tokens for output */
    readonly maxTokens?: number;
    /** Temperature */
    readonly temperature?: number;
}
/**
 * Interface for Model Adapters.
 */
export interface ModelAdapter {
    generate(prompt: string): Promise<string>;
}
/**
 * A structured insight generated by AI.
 */
export interface Insight {
    /** Unique ID for the insight */
    readonly id: string;
    /** Title of the insight */
    readonly title: string;
    /** Detailed description */
    readonly description: string;
    /** Severity level */
    readonly severity: 'low' | 'medium' | 'high' | 'critical';
    /** Category of the issue */
    readonly category: 'performance' | 'reliability' | 'security' | 'architecture';
    /** Suggested action */
    readonly suggestion: string;
    /** Related microservices/functions */
    readonly relatedItems: Array<{
        service: string;
        function: string;
    }>;
}
/**
 * Result of the AI analysis.
 */
export interface AnalysisResult {
    /** Timestamp of analysis */
    readonly generatedAt: number;
    /** List of generated insights */
    readonly insights: Insight[];
    /** Summary of the analysis */
    readonly summary: string;
    /** Raw model output (for debugging) */
    readonly rawOutput?: string;
}
/**
 * Input for the analyzer.
 */
export interface AnalysisInput {
    readonly report: HistoricalReport;
    /** Optional context (e.g., recent changes, specific focus) */
    readonly context?: string;
}
//# sourceMappingURL=types.d.ts.map